{
  "name": "wedding",
  "version": "1.0.0",
  "description": "A wedding application with user authentication",
  "main": "index.js",
  "scripts": {
    "start": "node api/index.js",
    "build": "rm -rf public && npm install && cd app && npm install && npm run build && cd .. && mv app/dist public"
  },
  "keywords": [
    "wedding",
    "authentication",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^13.4.0",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.5.0",
    "multer": "^2.0.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.0.1"
  }
}
